version: 2

models:
  - name: stg_customers
    columns:
     - name: customer_id
       description: "{{doc('customers')}}"
       tests:
       - unique
       - not_null
     - name: firstName
        - not_null
        #- accepted_values:
        #   values: ['values']
     
  