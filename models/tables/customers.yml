version: 2

models:
  - name: stg_customers
    columns:
     - name: customer_id
       tests:
       - unique
       - not_null
     - name: firstName
        - not_null
        #- accepted_values:
        #   values: ['values']
     
  